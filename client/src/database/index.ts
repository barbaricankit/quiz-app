import { signUp, signIn, getQuizData, verifyToken, setupAuthHeader } from './servercalls';
import { rules } from './rules.data';
export { signIn, signUp, getQuizData, verifyToken, setupAuthHeader, rules };
